diff --git a/kernel/core_hook.c b/kernel/core_hook.c
index f64e6a15..cb1195a2 100644
--- a/kernel/core_hook.c
+++ b/kernel/core_hook.c
@@ -859,7 +859,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_SUS_PATH -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_sus_path((struct st_susfs_sus_path __user*)arg3);
+			error = susfs_add_sus_path((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_SUS_PATH -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -875,7 +875,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_SUS_PATH_LOOP -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_sus_path_loop((struct st_susfs_sus_path __user*)arg3);
+			error = susfs_add_sus_path_loop((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_SUS_PATH_LOOP -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -891,7 +891,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_set_i_state_on_external_dir((char __user*)arg3, CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH);
+			error = susfs_set_i_state_on_external_dir((void __user*)arg3, CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH);
 			pr_info("susfs: CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -907,7 +907,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_SET_SDCARD_ROOT_PATH -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_set_i_state_on_external_dir((char __user*)arg3, CMD_SUSFS_SET_SDCARD_ROOT_PATH);
+			error = susfs_set_i_state_on_external_dir((void __user*)arg3, CMD_SUSFS_SET_SDCARD_ROOT_PATH);
 			pr_info("susfs: CMD_SUSFS_SET_SDCARD_ROOT_PATH -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -955,7 +955,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_SUS_KSTAT -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_sus_kstat((struct st_susfs_sus_kstat __user*)arg3);
+			error = susfs_add_sus_kstat((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_SUS_KSTAT -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -971,7 +971,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_UPDATE_SUS_KSTAT -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_update_sus_kstat((struct st_susfs_sus_kstat __user*)arg3);
+			error = susfs_update_sus_kstat((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_UPDATE_SUS_KSTAT -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -987,7 +987,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_SUS_KSTAT_STATICALLY -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_sus_kstat((struct st_susfs_sus_kstat __user*)arg3);
+			error = susfs_add_sus_kstat((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_SUS_KSTAT_STATICALLY -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -1005,7 +1005,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_TRY_UMOUNT -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_try_umount((struct st_susfs_try_umount __user*)arg3);
+			error = susfs_add_try_umount((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_TRY_UMOUNT -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -1023,7 +1023,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_SET_UNAME -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_set_uname((struct st_susfs_uname __user*)arg3);
+			error = susfs_set_uname((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_SET_UNAME -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -1054,7 +1054,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_SET_CMDLINE_OR_BOOTCONFIG -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_set_cmdline_or_bootconfig((char __user*)arg3);
+			error = susfs_set_cmdline_or_bootconfig((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_SET_CMDLINE_OR_BOOTCONFIG -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -1072,7 +1072,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_ADD_OPEN_REDIRECT -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_add_open_redirect((struct st_susfs_open_redirect __user*)arg3);
+			error = susfs_add_open_redirect((void __user*)arg3);
 			pr_info("susfs: CMD_SUSFS_ADD_OPEN_REDIRECT -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
@@ -1129,7 +1129,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 				pr_err("susfs: CMD_SUSFS_SHOW_ENABLED_FEATURES -> arg5 is not accessible\n");
 				return 0;
 			}
-			error = susfs_get_enabled_features((char __user*)arg3, arg4);
+			error = susfs_get_enabled_features((void __user*)arg3, arg4);
 			pr_info("susfs: CMD_SUSFS_SHOW_ENABLED_FEATURES -> ret: %d\n", error);
 			if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
 				pr_info("susfs: copy_to_user() failed\n");
